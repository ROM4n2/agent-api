# 🤖 Agent Context: agent-api

异步任务 + Worker Pool + LLM 调用的 Go 后端服务。客户端提交 prompt，服务异步调用 DeepSeek 生成结果，通过轮询获取。

### 1. 🛠️ Ground Truth Verification Baseline
- **Primary Test Command**: `go test ./... -count=1`
- **Baseline Test Result**: ✅ 32 passed, 0 failed (Captured on 2026-09-02)
  - api: 11 · llm: 3 · store: 5 · worker: 6 · metrics: 2 · config: 5 · main: 无测试文件
- **Build Command**: `go build ./...` (✅ exit 0)
- **Run Command**: `go run .` (监听 :8080)。密钥来自环境变量或 `config.yaml`，二选一即可
- **Benchmark Command**: `go test -bench=. -benchmem ./worker/`
  - `BenchmarkPoolEnqueue` ≈ 1.5 µs/op · `BenchmarkRunAgent` ≈ 84 ns/op

### 2. 🗺️ Core Symbol & Architecture Map
- Entrypoint: `main.go` (`config.Load` → 装配依赖 store/worker/llm → 启动 HTTP)
- Config: `config/config.go` — `Load()` 合并「环境变量 > config.yaml」；文件可选，只支持扁平 `key: value`，未知 key / 缺冒号直接报错。`main.go` 把 `APIAuthKey` 注入 `api.NewHandler`
- HTTP Layer: `api/handler.go` — 业务路由 `POST /run`(202)、`GET /tasks/{id}`；运维/演示路由 `GET /healthz`、`GET /metrics`、`GET /`(Demo)；Go 1.22+ ServeMux 方法路由。后三者只走 `Recover → RequestID`，不受限流/鉴权影响
- Metrics: `metrics/metrics.go` — `sync/atomic` 计数（submitted/running/done/failed）+ Prometheus 文本格式；零第三方依赖
- Demo: `api/demo.go` + `api/demo.html` — `go:embed` 内嵌单页，提交 prompt 并轮询展示状态演进
- Storage: `store/store.go` — 内存 map + Mutex 状态机；`Create/Update/Complete/Get`
- Worker: `worker/pool.go` + `worker/agent.go` — 固定 size(3) goroutine 消费 `queue(chan string)`；`process` 内跑 `runAgent` 多步工具循环；`chatter` 接口便于测试注入
- LLM: `llm/client.go` — OpenAI 兼容 `chat/completions` 封装；`ChatWithTools` 支持 function calling；`*APIError` 表示非 2xx
- HTTP 中间件: `api/middleware.go` — `Recover → RequestID → Limit(20/s,突发40) → Auth(Bearer/API_AUTH_KEY)`
- Data Flow: `POST /run` → `store.Create`(pending) + `metrics.IncSubmitted` → `pool.Enqueue(id)`(立刻 202) → worker `Update(running)` → `runAgent`: `llm.ChatWithTools` ⇄ 执行工具(calc/time) → `Complete(done/failed)` + `metrics.IncDone/IncFailed` → 客户端轮询 `GET /tasks/{id}`

### 3. 🚨 Local Engineering Red Lines & Gotchas
- **先 Create 再 Enqueue**：Enqueue 后 worker 可能立刻执行，该 ID 必须已存在于 store（见 `api/handler.go:61-62`）。
- **store 用 Mutex 全段加锁**：map 并发写会 panic；`Update/Complete` 是读-改-写复合操作，必须整段加锁（`store/store.go:27-29`）。
- **密钥与错误不得外泄**：`task.Error` 经 `GET /tasks/{id}` 原样返回，只存粗粒度分类（如 "upstream error"）；错误全文只进日志（`worker/pool.go:99-101`）。
- **Task 结构体直接序列化给客户端**：任何字段都不能放上游响应体、密钥等内部信息（`store/store.go:36-37`）。
- **Enqueue 会阻塞 HTTP handler**：queue 缓冲 = worker 数，满了 `Enqueue` 阻塞调用方，异步退化为同步。生产应改 `select+default` 快速失败或换持久化队列（`worker/pool.go:21-22`）。
- **Stop 用 cancel 而非 close(queue)**：避免向已关闭 channel 发送 panic；worker `ctx` 派生自 `p.ctx`，Stop 时一并取消在飞请求（`worker/pool.go:27-29,92-95`）。
- **必须 `NewStore()/NewPool()` 构造**：零值 Store 的 map 为 nil，写入 panic；`chatter` 接口可注入不发真实请求的实现做测试。
- **配置优先级**：环境变量 > `config.yaml` > 零值。`DEEPSEEK_API_KEY` 两路都拿不到时 `main.run()` 返回 1；`BaseURL=https://api.deepseek.com`，`Model=deepseek-chat`。`config.yaml` 已被 gitignore。
- **`/metrics` 只暴露聚合计数**：禁止把任务内容或上游响应体写进指标（`metrics/metrics.go`）。
- **对外结构体必须显式写 json tag**：`store.Task` 不加 tag 时 `encoding/json` 会用 Go 字段名输出 `ID/Status/...`，前端按小写读就全变 `undefined`（曾致 Demo 全字段 undefined 且轮询不终止）。契约由 `api/task_contract_test.go` 钉死。
- **`DEEPSEEK_API_KEY` 绝不经过浏览器**：网页 token 框只承载 `API_AUTH_KEY`，存 `localStorage`。上游密钥只从环境变量或 `config.yaml` 加载。
- **运维路由不走鉴权**：`GET /healthz`、`/metrics`、`/` 只包 `Recover → RequestID`，确保探活与抓取可达；改动路由时勿把 Auth 误加回去。

### 4. 📚 Coding Vault Linkage
- Global Standards: `d:\Obsidian\Coding\AGENTS.md`
- Vault Search: `python d:\Obsidian\Coding\scripts\search-vault.py "<query>"`
