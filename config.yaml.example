# 复制为 config.yaml 后填写真实值。config.yaml 已被 .gitignore 忽略，不会误提交。
#
# 优先级：环境变量 DEEPSEEK_API_KEY / API_AUTH_KEY 会覆盖这里的同名配置。
# 只支持扁平的 "key: value"，不支持嵌套、列表和多行值（解析到这些会直接报错）。

deepseek_api_key: "sk-你的key"
api_auth_key: "你的服务端密钥"
